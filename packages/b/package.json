{
  "name": "nx-14.6.0-ignores-package.json-config-b",
  "version": "0.0.1",
  "description": "A mock child package",
  "scripts": {
    "build": "echo 'Running build script for package b'",
    "test": "echo 'Running test script for package b'"
  },
  "nx": {
    "targets": {
      "test": {
        "dependsOn": [
          "build"
        ]
      }
    }
  }
}
